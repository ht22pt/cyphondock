machine:
  python:
    version: 3.6.1
  services:
    - docker
test:
  pre:
    - docker-compose -f docker-compose.travis.yml up --build -d
    - docker ps -a
    - sleep 30
  override:
    - ./scripts/runtests.sh
